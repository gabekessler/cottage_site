!!!
%html
  %head
    %title Cottage
    = stylesheet_link_tag :all
    = javascript_include_tag :defaults
    = csrf_meta_tag
  %body
    %p.notice= notice
    %p.alert= alert
    %ul
      - @pages.each do |page|
        %li= link_to "#{page.title}", page_url(page), :title => "#{page.title}"
    %h1= @page.title
    = yield
